# MAN 0.0.1（Windows x64）

版本线重置后的首个正式版本。

## 安装包

- 文件：`MAN-0.0.1-win-x64.exe`（143,635,897 字节）
- SHA-256：`11a10ec57d6c844c6a7495998a5b943c06603d782a1389022417dc4e59551859`
- 按产品规则公开发布不使用 Authenticode 代码签名；完整性以上述 SHA-256 为准。

## 更新线路

- 中国大陆：客户端根据公网 IP，使用 GitHub 加速代理获取 latest.json 和安装包。
- 其他地区：使用 GitHub Release 直连地址。
- 只有 latest.json 的 status 为 published 且对应平台 available=true 时，客户端才会显示更新。

## 本版本主要内容

- 版本线整体重置为 `0.0.1`，内置三份同步重签的 `0.0.1` 签名配置包。
- 实时 Overlay 运动外推：分析坐标短暂滞后时按速度投影，替代旧的过期即隐藏策略。
- 监视器窗口几何记忆：跨会话恢复窗口位置并按屏幕 clamp，防止拔掉显示器后窗口丢失。
- 帧新鲜度门控：偏移超过阈值的历史帧坐标不再画到当前画面。
- VC OCR 预热与重试修正、possession 门控锚点迁移、`config_id` 门禁。

## 发布边界

- 本版本仅提供 Windows x64 安装包；macOS 未随本版本发布。
- 真实干净机器安装、DPAPI 与真实设备验收仍在进行中。
