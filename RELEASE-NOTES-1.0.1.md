# MAN 1.0.1 更新说明

这次更新针对篮球27开放场模式的 asset2 识别进行了优化。

- 篮球27 `asset2` 替换为新的识别模板并重新签名，提升当前画面下的命中稳定性。
- 保留 `asset2_loading_spinner` 辅助识别探针，进入流程的辅助判断逻辑不变。
- 从 1.0.0 更新时优先下载增量包；失败会自动改用完整更新包。

注意：更新下载完成后，可按自己的时间重启安装；本地配置和数据会保留。
