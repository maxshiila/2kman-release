# MAN 1.0.10

本次更新是 1.0.9 的紧急加固版，聚焦 **Xbox 长时间运行的稳定性**，建议 Xbox 用户尽快升级。

## 🎮 Xbox 会话保活（重点）

- 修复 Xbox 串流会话被平台回收后，游戏内设置（投篮条、球员指示器等）被重置的问题：此前每次会话重连都会生成"全新手柄"，2K 按手柄硬件实例记忆设置，导致自动化依赖的设置丢失。
- 客户端现在每 2 分钟自动续期串流会话、长时间无操作时发送游戏感知不到的防闲置信号（低于死区的右摇杆微推），会话不再被中途回收，设置跨局保留。
- 平台主动断开串流时，客户端现在显示明确的停止原因，不再表现为莫名的传输中断。

## 🛡️ 丢包卡死修复

- 修复网络丢包出现花屏时，客户端可能整体卡死的问题：花屏检测日志不再可能刷爆解码事件循环，画面在关键帧恢复后自动回到正常状态。
- 花屏标记不再用于丢弃画面（实测正常帧误标率极低），避免好的帧被跳过造成额外卡顿。

## 📦 安装与更新

- 覆盖安装即可直接升级，无需卸载；配置已随包内置，装完即生效。
- 从 1.0.9 更新优先下载增量包；失败会自动改用完整更新包。
- 更新失败时的"前往下载页"现在指向官网安装指南（大陆可直连、镜像加速），不再跳转 GitHub。
